apiVersion: v1
kind: Service
metadata:
  name: fastapi-service
spec:
  selector:
    app: fastapi-webapp   
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30080     #lan port
  type: NodePort          #outside access
